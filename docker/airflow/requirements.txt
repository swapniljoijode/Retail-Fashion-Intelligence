# Dependencies installed into the custom Airflow image.
# Keep versions consistent with pyproject.toml where possible.

# Orchestration — Cosmos renders the dbt project as native Airflow tasks
astronomer-cosmos[dbt-duckdb]>=1.5.0,<2.0.0

# dbt adapters
dbt-core>=1.8.0,<2.0.0
dbt-duckdb>=1.8.0,<2.0.0

# Data generation
faker>=25.0.0
numpy>=1.26.0
pandas>=2.2.0
pyarrow>=15.0.0

# Storage and warehouse
boto3>=1.34.0
duckdb>=0.10.0

# Config
python-dotenv>=1.0.0
